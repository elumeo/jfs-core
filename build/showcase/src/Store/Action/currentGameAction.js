import { createStandardAction } from 'typesafe-actions';
const featureName = 'currentGame';
export const currentGameUpdateAction = createStandardAction(featureName + '/UPDATE_ROOM')();
//# sourceMappingURL=currentGameAction.js.map