export type Language = 'de' | 'en' | 'it';

export enum LANGUAGE {
  GERMAN = 'de',
  ENGLISH = 'en',
  ITALIAN = 'it',
}

export enum DATE_FORMAT {
  DE = 'dd.MM.yyyy',
  EN = 'dd/MM/yyyy',
  ES = EN,
  FR = EN,
  NL = 'dd-MM-yyyy',
  IT = EN,
}
