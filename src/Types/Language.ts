export type Language = 'de' | 'en' | 'it' | 'pl' | 'es';

export enum LANGUAGE {
  GERMAN = 'de',
  ENGLISH = 'en',
  ITALIAN = 'it',
  POLISH = 'pl',
  SPANISH = 'es',
}

export enum DATE_FORMAT {
  DE = 'dd.MM.yyyy',
  EN = 'dd/MM/yyyy',
  ES = EN,
  FR = EN,
  NL = 'dd-MM-yyyy',
  IT = EN,
  PL = 'yyyy-MM-dd'
}
