import React from 'react'
// import { useClippy, ClippyProvider } from 'clippyjs';

type Props = {}
const Clippy: React.FC<Props> = (props) => {
  return <></>
  // return <ClippyProvider></ClippyProvider>
}
export default Clippy
